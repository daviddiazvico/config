export CLICOLOR=1
export EDITOR=vi
export VISUAL=vi

# Aliases
alias autoupdate="sudo apt-get update && sudo apt-get dist-upgrade -y && sudo apt-get autoremove -y && sudo apt-get autoclean && sudo apt-get purge && sudo apt-get clean"

# ssh-agent
#eval $(ssh-agent -s)
#ssh-add ~/.ssh/id_rsa

# Miniconda3
#export PATH="/home/$USER/miniconda3/bin:$PATH"

# Neptune.ai
#export NEPTUNE_API_TOKEN="XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX"
